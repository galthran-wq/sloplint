[package]
name = "sloplint_diagnostics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rule-independent diagnostic abstractions (code, message, range, severity)."

[dependencies]
ruff_text_size.workspace = true

[lints]
workspace = true

# Classified as production (is_test = false): test-shaped contents are ignored, so it
# contributes only its size — no test functions, assertions, or assertion-free tests.
def test_looks_like_a_test():
    assert True

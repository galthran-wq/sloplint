//! `SLP` code → rule mapping (cf. Ruff's `codes.rs`).
//!
//! Filled in by the diagnostics/registry PR. Codes are the stable public identifiers
//! users put in config and inline suppressions, so they must never be renumbered.

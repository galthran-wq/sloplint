//! `cargo dev`-style developer utilities (cf. Ruff's `ruff_dev`).
//!
//! Will host code generation (rule docs, JSON schema) and rule scaffolding. A stub for
//! now so the structure exists.

fn main() {
    eprintln!("sloplint_dev: no commands yet — code generation lands with the registry PR");
}
